'use client'

import { useState } from "react";

const OpenGeneratorButton = () => {
    const [open, setOpen] = useState(false);
    return <button>Open Generator</button>
}

export default OpenGeneratorButton;